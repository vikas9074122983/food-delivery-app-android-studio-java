<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.ganeshatech18102021.fooddelivery">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodDelivery">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".AddressPickMap"
            android:label="@string/title_activity_address_pick_map"></activity>
        <activity android:name=".user.ConfirmOrderPage" />
        <activity android:name=".user.cartpage" />
        <activity android:name=".user.productpage" />
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".restaurant.Receivedorder" />
        <activity android:name=".restaurant.RestSetting" />
        <activity android:name=".restaurant.manageItem" />
        <activity android:name=".restaurant.addItem" />
        <activity android:name=".admin.helpsetting" />
        <activity android:name=".admin.aboutsetting" />
        <activity android:name=".admin.settingPage" />
        <activity android:name=".admin.AdminReport" />
        <activity android:name=".admin.ManageRest" />
        <activity android:name=".RestRegister" />
        <activity android:name=".user.HelpPage" />
        <activity android:name=".user.aboutCompany" />
        <activity android:name=".user.orderspage" />
        <activity android:name=".user.userInfo" />
        <activity android:name=".user.usersetting" />
        <activity android:name=".RestOwnerActivity" />
        <activity android:name=".ForgotPassActivity" />
        <activity android:name=".AdminHomepage" />
        <activity android:name=".UserHomepage" />
        <activity android:name=".Register" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>